/**
 * A CanvasCoordinate is an (x, y) point on the canvas.
 */

export class CanvasCoordinate {
    x: number;
    y: number;

    constructor(x: number, y: number) {
        this.x = x;
        this.y = y;
    }
}
